<div class="flex mt-40 pt-40">
  <div class="w-1/2 bg-white pt-10 px-20 mb-10 rounded-lg">
    <div class="">
      <span><%= @user.nickname %></span>
      <span>@<%= @user.name %></span>
    </div>
    <% if @user.id == current_user.id %>
        <%= link_to 'プロフォールを編集する', new_article_path, class: 'bg-custom-gray rounded-lg text-center py-2 w-full' %>
    <% end %>
  </div>
  <div class="w-1/2">

  </div>
</div>
      <% if user_signed_in? %>
        <%= button_to 'ログアウト', destroy_user_session_path, { method: :delete, data: { turbo_confirm: '本当に削除しますか？' } } %>
      <% end %>

