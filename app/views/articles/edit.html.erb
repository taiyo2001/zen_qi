<div class="p-2 h-screen-minus-header flex flex-col">
  <%= form_with(model: @article, url: article_path(@article), method: :put, data: { turbo: false }, html: { class: 'h-full' }) do |form| %>
    <%= render partial: 'form', locals: { form: } %>
  <% end %>
</div>
